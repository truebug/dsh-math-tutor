# 进度日志

> 倒序记录每次交付，与 git log 互补（含线上状态）。

## 2026-08-30

- **英语词库切换新版教材（P0 落地）**：用户拍照提供上教社 2024 新版《英语》
  二年级上册官方词汇表（docs/ref/english_g2.1.jpg / english_g2.2.jpg，
  2025 秋启用，12 单元 71 词条 + Mini Project）。12 个词汇关卡全部替换为
  新教材单元词库（关卡 ID 沿用旧主题，用户进度/星级无损），标题改为
  「Unit N 中文主题」；旧主题扩展词移出主线。IPA 映射补 20 个新教材词，
  总计 989 条，新词库音标覆盖率 100%。关卡 count=12 大于单主题词数
  （5-11 词），出题函数自动取 min 截断，已确认无异常。
- **课程表 PDF**：docs/ref/课程表.jpg 转录为打印版 HTML→PDF（Chrome
  headless），A4 纵向一张双面两张同款（docs/ref/output/，已 gitignore，
  不入库）。

## 2026-08-28

- **英语词库音标接入（旧牛津体系）**：`djfml/englishoxfordshanghaiedition`
  anki 牌组（1042 词带音标）解包提取 925 词条 + 手工补 44 个低年级高频词
  （问候短语/家庭成员等，标准 DJ 音标），生成 `lib/english-ipa.ts` 映射
  （969 词条）。词汇题英译中题干附音标展示（`"pen /pen/" 的中文意思是？`），
  现有 232 词覆盖率 100%。ANKI 牌组解析：anki21b = zstd 压缩 SQLite。

- **P0 教材核实·第二轮（Kimi 联网搜索突破）**：发现 Kimi coding 端点支持
  Anthropic 兼容 `POST /coding/v1/messages` + 服务端 `web_search_20250305`
  工具（搜索服务端执行，无需客户端回声；k3 仅接受 temperature=1；OpenAI
  协议 `$web_search` 回声会 400）。多轮搜索结论：**新版（2024 课标）二年级
  上册 2025 秋才启用，公开网络暂无数字版词表**——资料站只有旧牛津上海版
  二上（部分内容，腾讯文库）和沪教深圳版词表；2024 新沪教版初中词表确已
  上网（学科网/book118）。另发现资产：`djfml/englishoxfordshanghaiedition`
  （旧牛津上海小学全部单词带音标 anki deck）。获取路径不变：课本附录拍照 >
  智慧教育平台电子课本 > 等待网络流传。

- **P0 教材核实·联网检索记录**：Bing 网页检索（中文查询）结果噪音过大，
  未获得可用的现行教材目录/词表。可靠路径排序：① 国家中小学智慧教育平台
  （basic.smartedu.cn，官方电子课本，需人工或带会话抓取）；② 家长提供
  二上新版《英语》（上教社 2024 版）课本/词表照片；③ 已有新版三上扫描件
  `/tmp/hj_eng3a.pdf`（本机，词表 p67+ 待提取）。拿到权威词表前英语词库
  维持现状并标记「待核实」。

- **访客试玩 + 分享卡 + 触屏优化（P1/P2 体验批）**：
  - 访客零门槛试玩：onboarding 新增「先随便逛逛，试玩一关」——内存虚拟档案
    （不落库/不上传/不计星/不参与画像与勋章），地图/每日挑战/游乐场全可玩；
    结算页隐藏 AI 点评/对战/竞赛码，改为「创建专属档案」引导（刷新回流 onboarding）。
  - 成绩单分享卡（`components/ShareCard.tsx`）：Canvas 生成 640×800 羊皮纸风 PNG
    （昵称/日期/科目/星级/三大数字/鼓励语/站点地址），支持系统分享（Web Share API）
    与下载保存；访客也可用，利于 demo 传播。
  - 贪吃蛇触屏滑动转向（touchstart/touchend 位移阈值 24px 防误触），iPad 更跟手。
  - 音效此前已覆盖（答对/答错/连击/通关 WebAudio 合成），本轮确认无缺口。

- **每周成长报告可打印版**：看板原「纯文字简报」升级为结构化报告卡
  （`components/GrowthReport.tsx`）——4 项大数字（练习次数/题目/正确率/时长，
  均带周环比 ↑↓→）、7 天柱状图（柱=题数、金点=正确率）、分科对比表
  （本周/上周正确率 + 变化）、亮点（最佳一次/满分次数）+ 重点关注
  （最薄弱关卡 + 加练建议）、AI 老师寄语（复用 `/api/weekly-report`，保留朗读）。
  数据全部本地 sessions 确定性计算，零后端改动；「🖨️ 打印」走
  `@media print` 只输出报告卡、隐藏按钮，适 A4。
  顺带清理 DashboardView 重复的 getFamilyId import；旧 WeeklyReport 函数删除。

## 2026-08-27

- **昵称绑定 + 冲突处理（方案 C）**：服务器 `nicknames.json` 索引（昵称→familyId+
  可选 PIN 哈希），三个新路由：claim-nickname（同昵称 409 冲突）/resolve-nickname
  （PIN 校验）/set-pin（4-6 位数字，可清除）。前端：开启同步时自动登记昵称，
  冲突弹「是我本人恢复 / 重名新人新开」选择；恢复入口同时支持家庭 ID 和昵称；
  看板可选设 PIN 加锁。三线实测通过（绑定→冲突→PIN 拦截→PIN 放行）。
- **dsh 真运行时灰度三步全落地**（3a9b7e9/cc14e0d/f9f4e79）：
  - 本地验证：`dsh-jsonrpc-agent` spawn + cordis.yml 最小组合跑通 Kimi k3
    （llm-deepseek 适配器 baseURL 直指 Kimi 端点，无需自写适配器）。
  - hint：`/api/hint?provider=dsh` 线上跑通，session 持久化
    `/var/lib/dsh-tutor/sessions` 生效（服务器 Node v22.19 支持 zstd）。
  - review：`/api/review?provider=dsh` 跑通，familyId 会话锚点注入。
  - sprite：新增 `POST /api/sprite-advice`——agent 自主决策小精灵开口内容
    （实测自主结合错因统计与推荐关），dsh→kimi→前端本地规则三层降级；
    前端本地规则即时出泡、agent 回复后原地升级。
  - 部署形态：SDK client 按需 spawn 子进程非常驻服务；崩溃自动重孵。
  - 事故记录：一次部署误将前端 src 覆盖后端目录导致 404，已重铺修复
    （后端代码全在 git，无损）。
- **主动性补全 + 勋章系统**（84357c8）：结算页小精灵复盘邀请气泡
  （练习前建议→练习中干预→练习后复盘链路闭环）；14 枚勋章
  （集星/满星/连击打卡/累计答题/错题重练五线，纯本地判定），结算页新勋章
  弹窗 + 看板勋章墙（灰显带进度）；`scripts/recommend-review.mjs`
  月度推荐规则 review 报告（采纳率/命中率阈值告警）。
- **教材换版核实（高年级题库 P0 前置）**：确认牛津上海版英语已停用，
  2024 秋起上教社新版《英语》逐年替换（2026 秋三年级换新版）；
  从 ChinaTextbook 拿到 2024 审核版《英语（三年级起点）三上》扫描件，
  结构=4 Module × 12 Unit，书末分单元词表已定位待提取。
  数学/语文 curriculum 文档同步标注换版风险（数学待新版目录、
  语文待修订版词语表复核）。结论：存量二年级内容保留，
  新关卡一律以新教材词表为准。
  - 语文二上复核通过（53cc6c8）：依据 PDF 即 2024 修订版（版权页有章），
    目录篇目与 12 关映射逐一核对一致，语文可宣称同步。
- **三层契约落地**（5ec4988）：sprite.ts 决策入口 + agent.ts provider
  网关（AGENT_PROVIDER 可切，默认 kimi）+ roadmap 迁移映射表，
  为接 dsh/任意 agent 框架铺路，纯重构行为不变。
- **游乐场扩员**（394e417）：打地鼠×2（识字/单词）+ 翻牌记忆×2，
  游乐场共 11 关 5 玩法；看板 14 天指标趋势图（metrics.daily 按日切片）。

## 2026-08-26

- **游乐场大陆上线**（5578c5b）：藏宝图第四块大陆 🎡，7 关独立解锁链，
  智慧树面板同步收录，已部署 coolje00。
  - 单词消消乐 ×3 关（kind=match）：英文↔中文卡片配对消除，首点即中记对、
    错配记错；词库复用 VOCAB 主题（`vocabThemes()` 按关合并）。
  - 古诗词接龙 ×2 关（kind=poemchain）：上句接下句四选一连击，复用 POEMS
    相邻句对出题。
  - 数字贪吃蛇 ×2 关（kind=snake）：15×15 棋盘吃正确答案，撞墙/撞自己不
    扣分只复位（低幼友好）；触屏方向盘 + 键盘 WASD/方向键双操控；
    蛇速随档位 230/190/150ms。
  - 关键设计：所有游戏结束合成标准答题记录（questions/answers/perQuestionMs），
    星星、错题本、AI 点评（按各自学术科目）、积分榜零改动复用。
  - 解锁链修复：`recordStars` 解锁仪式从只认 STAGES 扩展为四大链全查。
  - 设计文档 `docs/arcade-games.md`：贪吃蛇判分细节 + 12 个候选玩法
    backlog（打地鼠/翻牌记忆/口算接龙/飞花令/塔防等，按成本/价值排序）。
- **关卡难度随年级**（1c6e226）：数学加减法关 grade≥3 上限升至 1000、
  grade≥4 启用小数；每日挑战数学分支同步。当前为预埋——onboarding
  3-5 年级仍 disabled，等高年级题库落地后自动生效。

## 2026-08-25

- **Agent 化路线 P0→P3 全落地**（对照 vision 反思 DSH 运行时零接入差距，
  计划见 `docs/roadmap-agent.md`，已部署 coolje00）：
  - P0 插件架构（3c89151）：服务端迁移最小化 cordis 约定宿主 `host.ts`，
    review/hint/battle/score/profile 五路由改造为 name+apply(ctx) 插件，
    index.ts 收敛为分发器，纯重构行为不变。
  - P1 画像注入（176a582）：`services/learnerCtx.ts` 服务端画像摘要
    （错因趋势/近期走向/节奏偏好），review/hint 支持 familyId 注入，
    agent 会话记住孩子。
  - P2 主动性（99ad3b5）：小精灵地图页画像驱动「今日建议」气泡
    （错因提醒/打卡催促/关卡推荐）+ 答题中连续秒错（<2.5s×2）主动提醒。
  - P2 难度自适应（c49cce2）：画像驱动题量 ±25%（`adaptiveStageTune`）。
  - P3 家长周报（c49cce2）：`routes/weekly.ts` LLM 一周画像简报 + 语音朗读。
  - P3 指标落库（8b3a338）：推荐采纳率/反哺命中率四埋点确定性统计，
    看板错因卡片底部展示。
- **积分系统**（5818104）：每日打卡连击加分、破个人记录 +20、破全服记录
  +50、满分 +5；掩码昵称排行榜（看板 Top20 + 我的排名）、结算页加分动效。
- **AI 点评语音朗读**（e37e20d）：点评 + 错题复盘朗读；语文类答题时禁读
  防泄答案，英语发音朗读保留。
- **视觉整改**（10490b2/d44dbe1/b5aff6d）：场景屏视口自适应
  （svh+宽高比断点+ResizeObserver 视差重算）、Hero 粒子星图背景
  （Canvas2D 零依赖可降级）、按钮玻璃流光微交互；一关一屏 100svh +
  scroll-snap 逐屏分屏；图片瘦身 17→8MB（1920px q78 webp + 去水印裁顶）、
  懒加载 + idle 预取、章节圆点 rail、ken-burns 活背景、返回顶部按钮。
- **智慧树快捷通道**（f63e25a）：Hero 树区热键 → 全关卡导航面板
  （年级行灰色占位/学科分栏/锁定与满星态/未来科目迷雾区）；
  onboarding 3-5 年级置灰（内容仅二年级）。
- **44 张 AI 关卡插画**：接入 `apps/web/public/stages/`（math 16 + chi 12 +
  eng 16），用户侧生成，本地 cwebp 转码。

## 2026-08-24

- **科目大陆制**：首页大陆选择器（数学 16 关 + 语文/英语迷雾占位），
  初始分路不做中间分支，选择持久化。
- **藏宝图英雄区**：羊皮纸 SVG（纸纹/罗盘玫瑰/虚线航线/X 标记/海怪），
  预埋 public/treasure-map.webp 生成静帧自动覆盖机制。

- **Phase 4 上线**：小数加减法三关（彩虹泉/星空驿站/月面城），缩放整数空间
  生成避免浮点误差；竞赛码 G4A；normalizeAnswer 支持小数与全角小数。

- **Phase 3 上线**：千以内多位数加减法三关（云端草原/沙漠绿洲/烈焰火山）；
  修复地图关卡 max 被硬编码为 81 导致 100 以内关卡实际只出到 81 的问题。
- **放弃退出**：答题页左上角 ✕（两段确认），误闯关卡不写记录/画像/对战成绩。
- **画像上云**：`/api/profile/<familyId>`（JSON 白名单、consent 必填）；
  成长看板加监护人同意卡片；换设备凭家庭 ID 恢复；AI 点评携带历史画像摘要。
- **解锁仪式 + 场景辨识**：新关解锁时地图自动滚动 + 雾气散开 + 光晕脉冲；
  钟乳石/雪顶/瀑布线/廊柱/缆索/竹林/波光地标剪影。

## 2026-08-23

- **首次上线**：coolje00 复用 2008 端口（/dsh-math-tutor/ + /api/ → 127.0.0.1:8787）。
- **AI 点评**：/api/review 匿名错题摘要 → LLM（已切换 Kimi k3，.env 四行换厂商）。
- **多人对战**：竞赛码即房间号，join/score/state，成绩页 2s 轮询排行榜。
- **错因聚类**：看错符号/进退位失误/计算错误，看板分布条 + 口诀建议。
- **画像反哺出题**：进退位正确率 <80% 自动加量（对战锁定参数）。
- **游戏化**：寻宝卷轴地图（三层山脊剪影视差）、十关、每日挑战（日期种子）、
  宠物养成（🥚→🐉）、连击火焰、Canvas 粒子（余烬/星星雨）。
- **Phase 1/2**：100 以内加减法、表内乘除法（含琐碎题过滤与进退位占比分级）。
- **MVP**：本地出题（种子确定性）+ 计时 + 自动判分 + 错题本 + 竞赛码。

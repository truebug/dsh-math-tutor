# 进度日志

> 倒序记录每次交付，与 git log 互补（含线上状态）。

## 2026-08-24

- **科目大陆制**：首页大陆选择器（数学 16 关 + 语文/英语迷雾占位），
  初始分路不做中间分支，选择持久化。
- **藏宝图英雄区**：羊皮纸 SVG（纸纹/罗盘玫瑰/虚线航线/X 标记/海怪），
  预埋 public/treasure-map.webp 生成静帧自动覆盖机制。

- **Phase 4 上线**：小数加减法三关（彩虹泉/星空驿站/月面城），缩放整数空间
  生成避免浮点误差；竞赛码 G4A；normalizeAnswer 支持小数与全角小数。

- **Phase 3 上线**：千以内多位数加减法三关（云端草原/沙漠绿洲/烈焰火山）；
  修复地图关卡 max 被硬编码为 81 导致 100 以内关卡实际只出到 81 的问题。
- **放弃退出**：答题页左上角 ✕（两段确认），误闯关卡不写记录/画像/对战成绩。
- **画像上云**：`/api/profile/<familyId>`（JSON 白名单、consent 必填）；
  成长看板加监护人同意卡片；换设备凭家庭 ID 恢复；AI 点评携带历史画像摘要。
- **解锁仪式 + 场景辨识**：新关解锁时地图自动滚动 + 雾气散开 + 光晕脉冲；
  钟乳石/雪顶/瀑布线/廊柱/缆索/竹林/波光地标剪影。

## 2026-08-23

- **首次上线**：coolje00 复用 2008 端口（/dsh-math-tutor/ + /api/ → 127.0.0.1:8787）。
- **AI 点评**：/api/review 匿名错题摘要 → LLM（已切换 Kimi k3，.env 四行换厂商）。
- **多人对战**：竞赛码即房间号，join/score/state，成绩页 2s 轮询排行榜。
- **错因聚类**：看错符号/进退位失误/计算错误，看板分布条 + 口诀建议。
- **画像反哺出题**：进退位正确率 <80% 自动加量（对战锁定参数）。
- **游戏化**：寻宝卷轴地图（三层山脊剪影视差）、十关、每日挑战（日期种子）、
  宠物养成（🥚→🐉）、连击火焰、Canvas 粒子（余烬/星星雨）。
- **Phase 1/2**：100 以内加减法、表内乘除法（含琐碎题过滤与进退位占比分级）。
- **MVP**：本地出题（种子确定性）+ 计时 + 自动判分 + 错题本 + 竞赛码。
